# 🚀 دليل البدء السريع - منصة Hasad

## ⚡ البدء السريع (5 دقائق)

### 1️⃣ تثبيت المتطلبات

```bash
npm install
```

### 2️⃣ تشغيل المشروع

```bash
ng serve
```

### 3️⃣ فتح المتصفح

افتح المتصفح على: `http://localhost:4200`

---

## 🔑 بيانات الدخول التجريبية

### حساب المزارع

- **البريد**: `farmer@hasad.com`
- **كلمة المرور**: `password123`

### حساب الشركة

- **البريد**: `company@hasad.com`
- **كلمة المرور**: `password123`

---

## 📂 هيكل المشروع

```
src/
├── app/
│   ├── core/              # الخدمات والنماذج الأساسية
│   ├── shared/            # المكونات المشتركة
│   ├── features/          # وحدات الميزات
│   │   ├── auth/         # المصادقة
│   │   ├── farmer/       # المزارع
│   │   ├── company/      # الشركة
│   │   ├── field-tasks/  # المهام الميدانية
│   │   ├── contracts/    # العقود
│   │   ├── reports/      # التقارير
│   │   └── notifications/# الإشعارات
│   └── app.module.ts
└── assets/               # الملفات الثابتة
```

---

## 🎯 الصفحات الرئيسية

### المزارع

- **Dashboard**: `/farmer/dashboard`
- **الأراضي**: `/farmer/lands`
- **تفاصيل الأرض**: `/farmer/lands/:id`

### الشركة

- **Dashboard**: `/company/dashboard`
- **البلوكات**: `/company/blocks`

### المشتركة

- **المهام الميدانية**: `/field-tasks`
- **العقود**: `/contracts`
- **التقارير**: `/reports`
- **الإشعارات**: `/notifications`

---

## 🛠️ أوامر مفيدة

### تطوير

```bash
ng serve              # تشغيل وضع التطوير
ng serve --open       # تشغيل وفتح المتصفح تلقائياً
ng serve --port 4300  # تشغيل على منفذ مختلف
```

### بناء

```bash
ng build              # بناء المشروع
ng build --prod       # بناء للإنتاج
```

### اختبار

```bash
ng test               # اختبارات الوحدة
ng e2e                # اختبارات النهاية للنهاية
```

### توليد

```bash
ng generate component my-component    # مكون جديد
ng generate service my-service        # خدمة جديدة
ng generate module my-module          # وحدة جديدة
```

---

## 📝 البيانات الوهمية

المشروع يستخدم بيانات وهمية (Mock Data) في الخدمات. للربط بـ Backend حقيقي:

1. افتح الخدمات في `src/app/core/services/`
2. استبدل `of()` بـ `this.http.get()` أو `this.http.post()`
3. أضف عنوان API الصحيح

مثال:

```typescript
// قبل (بيانات وهمية)
return of(mockData).pipe(delay(500));

// بعد (API حقيقي)
return this.http.get<Type>("https://api.hasad.com/endpoint");
```

---

## 🎨 التخصيص

### الألوان

عدّل الألوان الرئيسية في `tailwind.config.js`:

```javascript
colors: {
  primary: {
    50: '#f0fdf4',
    100: '#dcfce7',
    // ... باقي الدرجات
  }
}
```

### الأيقونات

المشروع يستخدم رموز تعبيرية (Emojis). يمكنك استبدالها بـ:

- Font Awesome
- Material Icons
- Custom SVG Icons

---

## 📚 الوثائق الكاملة

للمزيد من التفاصيل، راجع:

1. **README.md** - نظرة عامة شاملة
2. **PROJECT_COMPLETION_SUMMARY.md** - ملخص الإكمال
3. **ADVANCED_FEATURES.md** - المميزات المتقدمة
4. **DEVELOPER_GUIDE.md** - دليل المطور
5. **SETUP.md** - دليل التثبيت التفصيلي

---

## 🐛 حل المشاكل الشائعة

### المشكلة: أخطاء عند `npm install`

**الحل**: تأكد من تثبيت Node.js 14+ و npm 6+

### المشكلة: البورت 4200 مستخدم

**الحل**: استخدم منفذ آخر

```bash
ng serve --port 4300
```

### المشكلة: أخطاء TypeScript

**الحل**: قم بتحديث المكتبات

```bash
npm update
```

### المشكلة: أخطاء Tailwind CSS

**الحل**: أعد بناء المشروع

```bash
npm run build
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تحقق من console المتصفح للأخطاء
2. راجع ملفات التوثيق
3. تحقق من التعليقات في الكود

---

## ✅ قائمة التحقق الأولى

- [ ] تثبيت Node.js و npm
- [ ] تشغيل `npm install`
- [ ] تشغيل `ng serve`
- [ ] فتح `http://localhost:4200`
- [ ] تسجيل دخول بحساب تجريبي
- [ ] استكشاف جميع الصفحات
- [ ] قراءة الوثائق

---

## 🎉 أنت جاهز الآن!

المشروع مكتمل 100% وجاهز للعمل. استمتع بالتطوير! 🚀

---

**تم إنشاء هذا الدليل بواسطة GitHub Copilot**  
**آخر تحديث:** 2024
